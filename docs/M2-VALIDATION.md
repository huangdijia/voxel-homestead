# M2 农牧增量验证

日期：2026-09-05。本次交付为 M2 的农业与猪羊繁殖子集，完整主世界、其他维度和多人仍在后续覆盖表中；不表示 Minecraft Java 1.21.1 全功能对齐。

## 已实现规则

四类分阶段作物（小麦、胡萝卜、马铃薯、甜菜）、干湿耕地、附近静态水源灌溉、光照与生长、骨粉、收获、面包／烤马铃薯／甜菜汤、堆肥、铁桶移水、三种锄、剪刀、猪羊食物吸引／喂养／繁殖／幼体成长和羊毛再生。新世界使用生成器 2，生成自然草丛；旧世界保留生成器 1，仍可通过树叶堆肥、骨粉长草取得小麦种子。胡萝卜和马铃薯有僵尸稀有掉落来源，甜菜种子目前仅创造模式可取得。

光照、生长时序、动物寻路及有毒马铃薯效果采用本项目近似规则；水仍为静态水源。大规模牧场、跨浏览器自然游玩全过程和新生成器的 900 秒性能回归没有在本记录中宣称通过。

## 规则与故障回归

2026-09-05 08:52 的全量运行：**269 项通过，1 项默认跳过；13 个测试文件通过，1 个 CPU 基准文件跳过**。`pnpm build` 的 TypeScript 与 Vite 生产构建通过，主 JS 866.63 KB、gzip 242.16 KB。此统计早于随后全屏／PWA 优化；最终合并结果另见对应验证记录。

- `tests/farming.test.ts`：37 项，覆盖灌溉、光照、分阶段生长、掉落、随机状态保存和高度边界。
- `tests/agriculture-storage.test.ts`：59 项，覆盖农牧数据严格校验、版本 1 原样读取、升级为版本 2、生成器不变、升级前备份、两个 object store 的原子回滚、失败重试与独立导入。
- `tests/agriculture-integration.test.ts`：11 项，覆盖开垦—播种—水桶—骨粉—收获链、堆肥、食物容器归还、繁殖冷却、剪毛与资源守恒。
- `tests/agriculture-audit.test.ts`：9 个独立审查回归，涉及 60 Hz 浮点边界、未加载区掉落／堆肥／种植／动物交互、持锄开箱、墙角遮挡、开垦耐久以及 80 字符旧世界名称备份恢复。
- `tests/agriculture-visuals.test.ts`：10 项，检查实际农作物与特殊方块几何、碰撞及生成器兼容；几何测试本身不等于视觉验收。

## 真实浏览器旧存档升级

[原始记录](browser-migration.json)。Chrome 在 Vite 开发版真实加载由既有铁器工坊示例导入的独立测试世界，显式保存后确认：存档 1 → 2，生成器保持 1；库存、装备、修改方块与升级前完全相同；备份保存的是原始版本 1 数据。

随后实际点击世界列表的“升级前备份”，下载文件真实落盘为 13,941 字节。读取该文件，经正式校验和导入函数再次创建了独立的版本 1 世界。导入步骤由开发工具调用正式函数，未冒充键鼠文件选择。回读和升级采用真实 IndexedDB；事务中断与配额故障的结论来自上述故障注入测试。

## 内容清单

[逐项内容审计基线](parity/README.md) 固定第三方数据提交和哈希，列出 12 个域、5,953 条记录、43 个行为验收项及 10 个缺失域。逐项验收通过数仍为 0；条目数量仅证明固定来源枚举和本地映射，不证明官方全量完整性或功能完成。

## 历史基线

[M1 验证记录](VALIDATION.md) 的 59.99887 FPS、18.2 ms p95 来自旧生成器、旧渲染版本的 900 秒真实浏览器基准。本次新增农业几何与动物状态后没有复用这组数字作为新版本性能通过结论。
